{
  "name": "plausible-futures-api",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend API for Plausible Futures Ideation App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "cross-env NODE_ENV=test node --test tests/**/*.test.js",
    "test:win": "set NODE_ENV=test&& node --test tests/**/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sql.js": "^1.10.3",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
